# ConfigScope 项目申报书

## 项目简介

ConfigScope 是一个使用 MoonBit 编写的配置契约演进兼容性检查器。它比较旧版本和新版本已经生成的 JSON 配置快照，识别新增、值变化、删除和类型变化，并判断这些变化对版本升级的影响，帮助开发者在发布前发现配置兼容性风险。

## 核心功能

- 对 JSON 配置进行确定性的递归比较；
- 将变化分类为 `compatible`、`behavioral` 和 `breaking`；
- 输出稳定的路径、变化类型和统计结果，但默认不暴露配置值；
- 提供 `compat` CLI，可直接用于发布检查；
- 支持 `breaking`、`behavioral`、`any` 三种发布门禁策略，并通过退出码接入 CI。

## 与 MoonConfigKit 的区别

MoonConfigKit 主要负责配置文本的解析、分层视图构建、合并和校验。ConfigScope 不实现 INI/Properties 解析、运行时分层合并或通用审计，而是消费已经生成的 JSON 快照，专注于判断两个配置发布版本能否安全升级。

因此，两者是可以串联使用的：前者负责生成和校验配置视图，后者负责在版本发布前检查配置契约是否发生不兼容变化。

## 项目价值与计划

ConfigScope 面向配置变更容易被忽略、但会影响线上服务启动和行为的实际问题，提供比普通差异展示更明确的兼容性结论和发布门禁。项目已包含 MoonBit 测试、文档示例、GitHub Actions 基础 CI 和 Apache-2.0 开源协议。

下一步将加入版本化配置契约文件和 GitHub Actions 兼容性发布门禁，进一步降低接入成本。项目不是现有代码移植，核心 API 面向 MoonBit 独立设计。
