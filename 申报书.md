# ConfigScope 项目申报书

## 基本信息
- 项目名称：ConfigScope：配置契约演进兼容性检查器
- 参赛者：Noverberrain；联系邮箱：2455251391@qq.com；GitHub 用户名：Noverberrain
- 项目仓库：https://github.com/Noverberrain/ConfigScope
- 项目方向：MoonBit 开发者基础设施 / 发布安全；原创项目，非移植项目

## 项目简介
ConfigScope 使用 MoonBit 比较候选版本与多个历史版本的 JSON 配置快照，把新增、值变化、删除和类型变化划分为 `compatible`、`behavioral` 或 `breaking`，帮助项目在发布前发现普通文本 diff 无法直接判断的配置兼容性风险。

## 核心功能与边界
- 提供确定性的递归比较、稳定路径排序、统计信息，以及默认不暴露配置值的文本和 JSON 报告；
- 提供 `compat`、`compat-matrix`、`contract-init`、`contract-check` CLI，并以退出码接入发布门禁；
- 契约支持多历史基线、`breaking` / `behavioral` / `any` 策略，以及精确路径和末尾 `.*` 子树忽略规则；
- 项目只消费已生成的 JSON 快照，不负责 INI/Properties 解析、运行时分层合并或通用配置校验。MoonConfigKit 可负责生成配置视图，ConfigScope 负责跨版本兼容性与发布门禁，两者边界互补。

## 验收说明
评审者可在仓库根目录直接执行：
```text
moon update && moon check --deny-warn --warn-list +73 && moon test --deny-warn && moon info
moon run cmd/main -- contract-check configscope.contract.json
.\demo.ps1  # Linux/macOS 使用 bash demo.sh
```
- 当前共 124 项测试，覆盖兼容、破坏、类型变化、多基线、无效清单、缺失文件和忽略规则，全部通过；
- 破坏性测试清单会返回非零退出码；GitHub Actions 自动执行检查、测试、接口校验、契约门禁、演示脚本及 Mooncakes 独立安装验证；
- 验收产物包括可复用核心库、CLI、版本化契约示例、README、API 接口文件、Apache-2.0 许可证和已发布的 Mooncakes 包。

## 原创说明与后续方向
本项目不复制 MoonConfigKit 或其他项目代码，核心模型、API 和 CLI 均面向 MoonBit 独立设计。后续仅沿配置兼容性方向完善发布策略、历史基线管理和报告集成，不扩展配置文本解析与运行时合并能力。
