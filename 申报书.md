# ConfigScope 项目申报书

## 基本信息

- 项目名称：ConfigScope：配置契约演进兼容性检查器
- 作者 / 参赛者：Noverberrain
- 联系邮箱：2455251391@qq.com
- GitHub 用户名：Noverberrain
- GitHub 仓库：https://github.com/Noverberrain/ConfigScope
- 项目方向：MoonBit 开发者基础设施 / 发布安全
- 是否为移植项目：否

## 项目简介

ConfigScope 是一个使用 MoonBit 编写的配置契约演进兼容性检查器。它比较候选版本和多个历史版本已经生成的 JSON 配置快照，识别新增、值变化、删除和类型变化，并判断这些变化对版本升级的影响，帮助开发者在发布前发现配置兼容性风险。

实际使用中，配置升级往往先于代码升级发生；普通文本 diff 能显示“哪里变了”，却不能直接说明“这次变化是否会阻止旧配置继续工作”。ConfigScope 面向发布前检查这一环节，把配置变化转换为稳定的兼容性结论和可接入 CI 的发布门禁。

## 核心功能

- 对 JSON 配置进行确定性的递归比较；
- 将变化分类为 `compatible`、`behavioral` 和 `breaking`；
- 输出稳定的路径、变化类型和统计结果，但默认不暴露配置值；
- 提供 `compat` CLI，可直接用于发布检查；
- 提供 `compat-matrix` CLI，检查一个候选版本与多个历史基线的兼容性；
- 提供版本化契约清单和 `contract-check` CLI，减少重复的命令行配置；
- 支持 `breaking`、`behavioral`、`any` 三种发布门禁策略，并通过退出码接入 CI。

## 预期验收产物

- 可复用的 MoonBit 核心库，以及 `compat`、`compat-matrix`、`contract-check` CLI；
- 可直接运行的版本化契约清单示例、兼容性报告和多基线检查结果；
- 覆盖兼容、破坏、类型变化和文件缺失场景的测试与测试夹具；
- GitHub Actions、README、API 接口文件和 Apache-2.0 许可证等完整仓库材料。

## 验收说明

验收以仓库中的可运行源码、测试、示例和 GitHub Actions 为准，不依赖人工演示环境。评审者可以在仓库根目录执行：

```text
moon update
moon check --deny-warn --warn-list +73
moon test --deny-warn
moon info
moon run cmd/main -- contract-check configscope.contract.json
```

验收标准如下：

1. 项目能够通过 MoonBit 检查和测试，当前测试套件为 102 项，全部通过且不产生被禁止的警告；
2. `contract-check` 能读取版本化契约清单，比较候选配置与多个历史基线，并输出每个基线的兼容性结论和汇总结果；
3. 没有超出所选门禁等级的变化时命令返回成功；使用 `cmd/main/testdata/configscope-breaking.contract.json` 时返回非零退出码，可以直接阻止 CI 或发布流程；
4. `cmd/main/testdata` 提供兼容、破坏和文件缺失场景，能够复核正常流程和错误处理；
5. GitHub Actions 会在 `main` 分支推送和 Pull Request 时自动执行依赖更新、检查、测试、生成接口校验和契约门禁。

## 与 MoonConfigKit 的区别

MoonConfigKit 主要负责配置文本的解析、分层视图构建、合并和校验。ConfigScope 的主入口不实现 INI/Properties 解析、运行时分层合并或通用审计，而是消费已经生成的 JSON 快照，专注于判断两个配置发布版本能否安全升级。早期的 merge/layer/audit API 仅保留在显式的 `legacy` 包中用于迁移，不属于主 CLI。

因此，两者是可以串联使用的：前者负责生成和校验配置视图，后者负责在版本发布前检查配置契约是否发生不兼容变化。

## 原创与参考说明

本项目不是对 MoonConfigKit 或其他项目的代码移植，也不复制配置文本解析、运行时分层合并等实现。ConfigScope 的独立边界是消费已经生成的 JSON 配置快照，专门完成跨版本兼容性判断、报告和发布门禁；核心模型、API 和 CLI 均面向 MoonBit 独立设计。

## 项目现状与后续计划

当前版本已经完成兼容性模型、报告 API、多基线检查、契约清单、CLI、测试、文档和 CI 契约门禁，能够作为独立工具运行。ConfigScope 面向配置变更容易被忽略、但会影响线上服务启动和行为的实际问题，提供比普通差异展示更明确的兼容性结论和发布门禁。

后续计划是补充更多配置格式的导入适配，进一步降低接入成本；这些属于现有交付之外的扩展方向。
